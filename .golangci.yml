version: "2"

run:
  timeout: 5m
  exclude-dirs:
    - vendor
    - contracts
    - apps
    - plugins
    - sdks
    - docs
    - scripts
    - config
    - migrations

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - bodyclose
    - misspell
    - gocritic
  settings:
    gocritic:
      enabled-checks:
        - dupBranchBody
        - dupCase
        - dupSubExpr

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
